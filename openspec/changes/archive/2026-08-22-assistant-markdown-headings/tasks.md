> **One change unit, one file pair.** `lib/contexts/assistant/presentation/assistant_markdown.dart`
> and `test/contexts/assistant/presentation/assistant_markdown_test.dart` compile and
> test only together — the tests import the block kinds and fields the same edit
> introduces. Do not split the parser, the widget, and the tests across isolated
> worktrees: each would see a file the other half has not written yet.

## 1. Parser: heading and thematic-break blocks

- [x] 1.1 In `assistant_markdown.dart`, add `heading` and `thematicBreak` to `MdBlockKind` and add a nullable `int? headingLevel` to `MdBlock` — a field distinct from `level` (design D1), wired into `==`, `hashCode` and `toString`; verify `flutter analyze` is clean and the existing `assistant_markdown_test.dart` still passes unchanged.
- [x] 1.2 Add the two line rules to `parseAssistantMarkdown`, matched **before** the bullet rule (design D2): `^\s*-{3,}\s*$` → a `thematicBreak` block with no spans and no marker; `^\s*(#{1,4})[ \t]+(\S.*)$` → a `heading` block whose content goes through `parseInlineMarkdown` and whose `headingLevel` is the `#` count; verify with parser tests in `assistant_markdown_test.dart` that `### 本月支出` yields one heading block, level 3, spans `['本月支出']`, and that `---` yields one `thematicBreak` block.
- [x] 1.3 Guard the deliberate limits with parser tests: `##### 太深` and `#tag` stay single-span paragraphs holding their literal text; `- 醫療` is still a bullet; `-----` is one break; `### **總計**` yields two spans with the second bold; verify each, then run the design-D8 mutations one at a time and confirm the named test goes red — `#{1,4}`→`#{1,2}`, `#{1,4}`→`#{1,5}`, `[ \t]+`→`[ \t]*`, `-{3,}`→`-{1,}`, and moving the break rule after the bullet rule. Restore the file from a `cp` backup between mutations.
- [x] 1.4 Re-point the existing `leaves syntax it does not know as literal text` test: drop `## 標題` (now a heading by design) and assert instead that `| a | b |`, `` `code` `` and `##### 太深` stay literal; verify the test passes and that deleting the heading rule makes the *new* heading tests red while this one stays green (the two tests must not overlap).

## 2. Widget: heading style, rule, spacing, semantics

- [x] 2.1 Build the heading text style in `AssistantMarkdown.build` from `effectiveStyle` — `FontWeight.w700` plus `fontSize * {1: 1.5, 2: 1.3, 3: 1.15, 4: 1.05}[headingLevel]` — with **no** `scaler.scale` on the font size (design D3, `Text` scales it itself), and render heading blocks through the marker-less branch of `_blockRow`; verify with a widget test asserting the four rendered `TextStyle.fontSize` values are strictly decreasing and all ≥ the paragraph's, and that a `### 本月支出` with no `**` renders at `FontWeight.w700` while a paragraph in the same reply does not.
- [x] 2.2 Render a `thematicBreak` block as a `scaler.scale(1)`-tall coloured box in the text colour at alpha 0.6 (chosen to clear the 3:1 non-text-contrast floor — see design D4), spanning `constraints.maxWidth` when bounded and `scaler.scale(160)` when not, with the comment saying why the unbounded case exists (design D4); verify with a widget test that a reply containing `---` between two paragraphs draws exactly one such box, that `find.textContaining('---')` finds nothing, and that its width equals the bubble's width in a width-constrained harness.
- [x] 2.3 Change `_gapAbove` to take the previous block and apply design D5's table (heading 14 above, break 12 above, 6 below a heading, 12 below a break, existing rules otherwise, first block still 0); verify with a widget test comparing the measured top offsets of a paragraph after a heading, a paragraph after a blank line, and a wrapped list item, asserting the heading gap is the largest of the three.
- [x] 2.4 Extend the semantics label: a heading contributes its plain text with no marker and no `－` level prefix; a thematic break is **filtered out** of the joined lines rather than joined as `''` (design D7); verify with a widget test that the bubble still exposes exactly one `Semantics` label, that it contains `本月支出`, and that it contains no `#` and no `-` run from those two source lines — then mutate the filter to emit `''` and confirm the test goes red.
- [x] 2.5 Guard that headings and rules leave the list layout alone (design D6): a reply of bullets → `### 小計` → bullets renders both bullet groups at the same horizontal offset, and a `### 小計` after a level-2 nested bullet starts at the block's leading edge; verify, then mutate by making `_markerWidth` count heading blocks and by storing the heading level into `MdBlock.level`, confirming each mutation turns the matching assertion red.
- [x] 2.6 Guard text scaling: render the same reply at `TextScaler.linear(1.0)` and `2.0` and assert every heading's painted height grows and stays ≥ body text's; verify, then mutate by wrapping the heading font size in `scaler.scale(...)` and confirm the resulting double-scaling is caught by an assertion distinct from the one that catches "not scaled at all".

## 3. Whole-change verification

- [x] 3.1 `flutter analyze` clean and `flutter test` fully green — confirm the run printed `All tests passed!`, not merely the absence of red.
- [x] 3.2 `TZ=UTC flutter test test/contexts/assistant/presentation/assistant_markdown_test.dart` green, matching the local (UTC+8) result.
- [ ] 3.3 Render a real sectioned reply (paragraph, `### 標題`, bullets, `---`, `#### 小標`, numbered list) in the assistant bubble on a narrow phone width at text scale 1.0 and 2.0 and confirm on device that the headings read as levels, the rule is visible, and nothing overflows horizontally.
